<%= simple_form_for(@meeting) do |f| %>
    <%= f.input :title, placeholder: "" %>
# continuer a mettre les parametres dans le simple form

    <%= f.button :submit, class: "btn btn-primary"  %>
<% end %>
